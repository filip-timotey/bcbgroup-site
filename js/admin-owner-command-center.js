import { supabase } from './supabase-client.js';
import { requireStaffContext,isOwnerProfile } from './admin-session.js';
const $=s=>document.querySelector(s);const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const labels={'users.manage':'Administrare utilizatori','employees.manage':'Administrare angajați','fleet.correct':'Corecții Fleet','site.manage':'Administrare site','data.manage':'Control date','projects.work':'Proiecte','time.work':'Pontaj & teren','crm.work':'CRM & oferte','fleet.work':'Fleet','media.work':'Media'};
function metric(icon,label,value,detail,state=''){return `<article class="occ-metric ${state}"><i class="fa-solid ${icon}"></i><div><span>${esc(label)}</span><strong>${esc(value)}</strong><small>${esc(detail)}</small></div></article>`}
async function load(){const btn=$('#occ-refresh');if(btn)btn.disabled=true;await supabase.rpc('refresh_bcb_operations_center');const [summary,caps,audit]=await Promise.all([supabase.rpc('owner_operations_summary'),supabase.from('role_capabilities').select('role,capability,enabled,updated_at').order('role').order('capability'),supabase.from('activity_log').select('id,action,entity_type,summary,created_at').order('created_at',{ascending:false}).limit(10)]);if(summary.error)throw summary.error;const s=summary.data||{},h=s.health||{};$('#occ-metrics').innerHTML=[metric('fa-heart-pulse','System Health',`${h.score??'—'}/100`,h.status||'fără snapshot',h.status==='healthy'?'good':'warn'),metric('fa-users','Acces activ',s.users?.active??0,`${s.users?.archived??0} conturi arhivate`),metric('fa-user-clock','Aprobări',s.users?.pending_access??0,'cereri în așteptare',s.users?.pending_access?'warn':'good'),metric('fa-bell','Notificări',s.notifications?.unread??0,'necitite pentru Owner',s.notifications?.unread?'warn':'good'),metric('fa-clock-rotate-left','Audit 24h',s.audit?.last_24h??0,'operațiuni înregistrate')].join('');const groups={admin:[],editor:[]};(caps.data||[]).forEach(x=>groups[x.role]?.push(x));$('#occ-capabilities').innerHTML=['admin','editor'].map(role=>`<div class="occ-role"><div class="occ-role-title"><strong>${role==='admin'?'Administrator':'Editor'}</strong><span>${groups[role].filter(x=>x.enabled).length}/${groups[role].length} active</span></div>${groups[role].map(x=>`<label class="occ-cap"><span>${esc(labels[x.capability]||x.capability)}</span><input type="checkbox" data-role="${role}" data-cap="${esc(x.capability)}" ${x.enabled?'checked':''}><i></i></label>`).join('')}</div>`).join('');$('#occ-capabilities').querySelectorAll('input').forEach(input=>input.addEventListener('change',async()=>{input.disabled=true;const {error}=await supabase.rpc('owner_set_role_capability',{p_role:input.dataset.role,p_capability:input.dataset.cap,p_enabled:input.checked});if(error){input.checked=!input.checked;alert('Permisiunea nu a putut fi actualizată.');}input.disabled=false;}));$('#occ-audit').innerHTML=audit.error?'<p>Audit indisponibil momentan.</p>':(audit.data||[]).map(x=>`<div><i class="fa-solid fa-circle"></i><span><strong>${esc(x.summary||x.action)}</strong><small>${esc(x.entity_type)} · ${new Intl.DateTimeFormat('ro-RO',{dateStyle:'medium',timeStyle:'short'}).format(new Date(x.created_at))}</small></span></div>`).join('')||'<p>Nu există activitate recentă.</p>';if(btn)btn.disabled=false;}
(async()=>{const ctx=await requireStaffContext({adminOnly:true});if(!ctx)return;if(!isOwnerProfile(ctx.profile)){location.replace('dashboard.html');return;}$('#occ-user').textContent=ctx.profile.full_name||'Owner BCB';$('#occ-refresh')?.addEventListener('click',()=>load().catch(console.error));await load();})().catch(e=>{console.error('Owner Command Center:',e);const b=$('#occ-refresh');if(b)b.disabled=false;});